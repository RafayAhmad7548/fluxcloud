name: fluxcloud
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  file_selector: ^1.0.3
  flutter_secure_storage: ^9.2.4
  dartssh2: ^2.13.0
  path: ^1.9.1
  file_picker: ^10.2.0
  path_provider: ^2.1.5
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
